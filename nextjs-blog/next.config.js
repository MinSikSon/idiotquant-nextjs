const nextConfig = {
    // experimental: {
    //     runtime: 'experimental-edge',
    // },
    // reactStrictMode: true,
    // swcMinify: true,
    images: {
        unoptimized: true
    },
}
module.exports = nextConfig;