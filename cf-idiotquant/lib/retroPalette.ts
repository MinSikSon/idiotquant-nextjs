// 게임 화면(90년대 기기)의 색.
//
// 게임 안에서만 쓰는 조각은 app/(game)/game/retro.tsx 에 있는데, 색만은 전역 내비게이션도
// 함께 쓴다 — /game 에 들어가면 위아래 바까지 같은 기기의 일부로 보여야 해서다. 그래서
// 색만 여기로 떼어 두 곳이 한 곳에서 읽는다. 값을 두 벌로 두면 어느 날 한쪽만 바뀐다.
//
// JSX 도 "use client" 도 없는 순수 상수 파일이다. 내비게이션은 모든 페이지에 들어가므로,
// 여기에 컴포넌트를 얹으면 게임을 안 여는 사람의 번들까지 커진다.

// 창 몸통은 **밝은** 회색이고 글자는 검정이다. 이 시대 UI 가 그랬고, 어두운 창에
// 밝은 글자를 쓰면 브라운관(정말 검은 자리)과 구별이 안 된다 — 어디가 화면이고 어디가
// 기계인지가 흐려진다. 검은 것은 차트 한 곳뿐이어야 한다.
export const R = {
    // 이 값은 app/global.css 의 html:has(#game-canvas) 규칙에도 적혀 있다. 고무줄 스크롤에
    // 드러나는 문서 뿌리를 칠하는 자리인데 CSS 가 TS 를 못 읽어서, 한쪽을 바꾸면 다른 쪽도 바꾼다.
    bg: "#0b0f10",        // 기기 밖 어둠
    face: "#a7b2a9",      // 창 몸통 — 검은 글자 대비 9:1
    faceLo: "#94a096",    // 창 안쪽 패널
    hi: "#d8e0d8",        // 밝은 모서리
    lo: "#4e5a53",        // 어두운 모서리
    bar: "#2f4f56",       // 타이틀바
    barHi: "#41686f",
    screen: "#0a1013",    // 브라운관 안
    ink: "#101614",       // 몸통 위 글자
    inkDim: "#3c4844",    // 몸통 위 흐린 글자 — 대비 4.6:1
    inkHi: "#e9f2ea",     // 타이틀바·브라운관 글자
    neon: "#5cf08f",      // 브라운관 안에서 살아 있는 값
    pink: "#ff5ec8",      // 기록 경신
    amber: "#e3b34a",     // 내 것 (기존 화면과 같은 금색)
} as const;
